{
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "spago build"
  },
  "devDependencies": {
    "spago": "^0.16.0",
    "purescript": "^0.13.0",
    "purescript-psa": "^0.7.2",
    "rimraf": "^2.5.0"
  },
  "dependencies": {
    "mkdirp": "^0.5.1"
  }
}
